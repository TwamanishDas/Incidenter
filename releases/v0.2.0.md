# Release Notes - v0.2.0

Working: Phase 2 | Step 4 | Version v0.2.0

## Release Summary

`v0.2.0` completes Phase 2 live telemetry execution:

1. Live Azure telemetry onboarding completed (Log Analytics, App Insights, Network Watcher, Monitor Metrics, Activity/Health).
2. Ingestion tuning completed:
   - `COLLECTION_INTERVAL=30`
   - `LOOKBACK_MINUTES=7`
   - `LOOKBACK_OVERLAP_SECONDS=45`
   - `INGESTION_DELAY_SECONDS=90`
   - `DEDUP_WINDOW_MINUTES=10`
3. Source coverage expanded:
   - `MONITOR_RESOURCE_IDS` expanded to 3 SQL-scope resource IDs.
   - Region overrides expanded to 3 `centralindia` mappings.
4. Contract/catalog alignment completed:
   - `LAYER_1_DATA_CONTRACT.md`
   - `TELEMETRY_CATALOG.md`
5. Phase 2 acceptance gate executed with evidence artifact:
   - `artifacts/phase2_step4_acceptance_latest.json`
   - Result: `pass` (12/12 criteria passed)

## Key Artifacts

1. Tracker:
   - `MASTER_PHASE_STEP_TRACKER.md`
2. Runbooks:
   - `PHASE2_STEP1_TELEMETRY_COLLECTION_KICKOFF.md`
   - `PHASE2_STEP2_INGESTION_TUNING.md`
   - `PHASE2_STEP3_SOURCE_COVERAGE_EXPANSION.md`
   - `PHASE2_STEP4_ACCEPTANCE_GATE.md`
3. Acceptance evidence:
   - `artifacts/phase2_step4_acceptance_latest.json`

## Tags

1. Checkpoint tag: `cp/p2-s4`
2. Release tag: `v0.2.0`

## Rollback

If rollback to Phase 2 checkpoint is required:

```powershell
git checkout cp/p2-s4
```

If rollback to Phase 2 release is required:

```powershell
git checkout v0.2.0
```
